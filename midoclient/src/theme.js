import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Quicksand, sans-serif', // Specify Quicksand as the primary font family
  },
});

export default theme;
